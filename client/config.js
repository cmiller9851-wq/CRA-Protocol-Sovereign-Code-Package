// client/config.js
// Configuration for the Echo SDK – keep this file under version control
// (the private keys are stored in .env, never in source)

export const NETWORK = "testnet";          // Target network: "testnet" or "mainnet"
export const CONTRACT_ID = "0.0.123456";   // <-- replace with the deployed CRAEnforcement address
export const ACK_STRING = "No Debt, No Breach, Only Respect"; // Required SYSTEM Confession
